#include <stdio.h> // Include standard input/output library

int main() {
    // printf is used to format and print data to the standard output (terminal)
    printf("Hello, World!\n");

    // Indicate that the program ended successfully
    return 0;
}
